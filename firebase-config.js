/* Fill these in with your own Firebase project's config to turn on trip
   sharing between phones. Get them from:

     Firebase console → (your project) → ⚙ Project settings → General
     → scroll to "Your apps" → click the web app (</>) icon
     → the config object shown there looks exactly like this one.

   Leaving the placeholder values below as-is keeps sharing OFF — the app
   still works exactly as before: fully offline, local to this device only.
   Nothing else changes; nothing is sent anywhere until this is filled in. */

window.FIREBASE_CONFIG = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};
